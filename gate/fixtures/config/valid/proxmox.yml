---
# Synthetic, gate-only fixture. See gate/fixtures/README.md. Not a real lab.
proxmox:
  api_host: "198.51.100.10"
  api_port: 8006
  node: "test-node"
  api_user: "homelab-infra@pve"
  api_token_id: "automation"
  api_token_secret: "test-token-secret-not-real"
  storage: "local-lvm"

networks:
  default:
    cidr: "198.51.100.0/24"
    gateway: "198.51.100.1"
    dns_servers:
      - "198.51.100.1"
    bridge: "vmbr0"
    vlan: 0
    ip_offset: 10
    max_hosts: 200
  shared:
    ip_offset: 20
    max_hosts: 10

ansible:
  ssh_user: root
  ssh_public_key: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAItest-not-a-real-key test@example"
