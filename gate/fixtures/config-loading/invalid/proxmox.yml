---
# Deliberately invalid: proxmox.node is absent and networks.default declares no gateway.
# gate/test-config-loading.sh asserts config-doctor.sh reports both as ERROR and exits 1.
proxmox:
  api_host: "203.0.113.10"
  api_port: 8006
  api_user: "homelab-infra@pve"
  api_token_id: "automation"
  api_token_secret: "fixture-not-a-real-secret"
  storage: "local-lvm"

networks:
  default:
    cidr: "198.51.100.0/24"
    dns_servers:
      - "198.51.100.1"
    bridge: "vmbr0"

ansible:
  ssh_user: root
  ssh_public_key: "ssh-ed25519 AAAAFixtureNotARealKey fixture@example.invalid"
