# Python toolchain for the homelab-infra lint/syntax gate. Installed into a WSL venv
# (see gate/README.md for the one-time bootstrap). Values are the exact
# versions pip resolved on first successful install — reproducible on a fresh WSL distro.
# Verified 2026-07-02: `pip freeze` after install matched these pins exactly.
# netaddr is the runtime dependency of the repo's ansible.utils ipaddr filters (added
# 2026-07-06; version matches what the venv already carries from a 2026-07-04 manual install).
ansible-core==2.18.1
ansible-lint==24.12.2
netaddr==1.3.0
yamllint==1.35.1
# proxmoxer and requests are the community.proxmox.proxmox module's own runtime deps
# (see its meta/ee-requirements.txt) and the community.proxmox inventory plugin's HTTP
# client; gate/test-proxmox-api-contract.py drives both for real against a job-local
# mock, so the gate venv needs them even though the syntax-check gates never did.
proxmoxer==2.3.0
requests==2.34.2
