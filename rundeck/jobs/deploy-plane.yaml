---
# Deploy Plane. Rendered and imported by rundeck/render-job.py.
- id: 1c5d6f93-0f5f-43b6-a0d7-182c5e31aa21
  uuid: 1c5d6f93-0f5f-43b6-a0d7-182c5e31aa21
  name: Deploy Plane
  group: Applications/Planning and Collaboration/Project Management/Plane
  description: |-
    Deploy or converge the **Plane** multi-container planning service.

    - **Dependencies:** named external PostgreSQL `postgresql-plane` and Redis `redis-plane`.
    - **Object storage:** a named local `plane-minio` service is part of this recovery unit;
      its persistent object-storage path is backed up with the Plane data directories.
    - **Secrets:** database, Redis, RabbitMQ, application, and object-storage credentials
      are generated or reused in Vaultwarden only.
    - **Recovery:** restore the named PostgreSQL database and the complete `/opt/<instance>`
      Compose data directory together. This job intentionally has no generic image rollback.
  loglevel: INFO
  executionEnabled: true
  scheduleEnabled: false
  nodeFilterEditable: false
  multipleExecutions: false
  options:
  - name: instance
    description: '**Default:** Keep the selected Plane instance. Enter another name for a second deployment.'
    required: true
    value: plane
  sequence:
    keepgoing: false
    strategy: node-first
    commands:
    - description: Run playbooks/apps/plane.yml
      script: |-
          #!/bin/bash
          set -euo pipefail
          : "${RD_OPTION_INSTANCE:?instance is required}"
          exec lab-run playbooks/apps/plane.yml -e "instance=$RD_OPTION_INSTANCE"
