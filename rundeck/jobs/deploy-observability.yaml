# Deploy Observability — generated companion to the Semaphore template of the same name.
# Load with:  rd jobs load --project homelab-infra --format yaml --file rundeck/jobs/deploy-observability.yaml
#
# The step calls `lab-run`, the single entry point installed by
# rundeck/bootstrap-rundeck.sh (a symlink to ansible/scripts/lab-run.sh in the checkout).
# It refreshes the checkout to origin/<branch>, validates config/ and execs
# ansible-playbook. Paths live in /etc/homelab-infra/lab-run.env, never in this file.

- id: 3c52788e-f659-5c2f-90f9-60ddf6144237
  uuid: 3c52788e-f659-5c2f-90f9-60ddf6144237
  name: Deploy Observability
  group: Apps
  description: |-
    Deploy or update Prometheus + Grafana as one app (Docker on monitoring_stack).
  loglevel: INFO
  executionEnabled: true
  scheduleEnabled: false
  nodeFilterEditable: false
  multipleExecutions: false
  sequence:
    keepgoing: false
    strategy: node-first
    commands:
    - description: Run playbooks/apps/observability.yml
      script: |-
          #!/bin/bash
          set -euo pipefail
          ARGS=()
          ARGS+=(-e "instance=observability")

          # lab-run refreshes the checkout, runs config-doctor, and execs
          # ansible-playbook through with-proxmox-env.sh. See ansible/scripts/lab-run.sh.
          exec lab-run playbooks/apps/observability.yml "${ARGS[@]}"
