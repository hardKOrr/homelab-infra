# homelab-infra

Project instructions live in `AGENTS.md` at the repo root, so that every agent tool reads
the same file. Keep them there — this file is a pointer, not a second source of truth.

@AGENTS.md
