---
# https://docs.ansible.com/projects/ansible/latest/collections/community/proxmox/proxmox_storage_module.html

- name: "storage.yml"
  gather_facts: true
  hosts: proxmox
  vars:

  tasks:
    - name: "Manage Proxmox Storage"
      community.general.proxmox_storage:
        api_host:
        api_password:
        api_port: 8006
        api_token_id:
        api_token_secret:
        api_user:
        cephfs_options:
        client_keyring:
        fs_name:
        monhost:
        password:
        path:
        subdir:
        username:
        cifs_options:
        domain:
        server:
        share:
        smb_version:
        content:
        dir_options:
        iscsi_options:
        portal:
        target:
        name:
        nfs_options:
        export:
        options:
        nodes:
        pbs_options:
        datastore:
        fingerprint:
        state:
        type:
        validate_certs:
        zfspool_options:
        pool:
