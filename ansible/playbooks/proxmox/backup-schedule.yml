---
# https://docs.ansible.com/projects/ansible/latest/collections/community/proxmox/proxmox_backup_schedule_module.html

- name: "backup-schedule.yml"
  gather_facts: true
  hosts: proxmox
  vars:

  tasks:
    - name: "Manage Proxmox Backup Schedule"
      community.general.proxmox_backup_schedule:
        api_host:
        api_password:
        api_port: 8006
        api_token_id:
        api_token_secret:
        api_user:
        backup_id:
        state:
        validate_certs:
        vm_id:
        vm_name:
