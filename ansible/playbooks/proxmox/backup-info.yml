---
# https://docs.ansible.com/projects/ansible/latest/collections/community/proxmox/proxmox_backup_info_module.html

- name: "backup-info.yml"
  gather_facts: true
  hosts: proxmox
  vars:

  tasks:
    - name: "Get Proxmox Backup Info"
      community.general.proxmox_backup_info:
        api_host:
        api_password:
        api_port: 8006
        api_token_id:
        api_token_secret:
        api_user:
        backup_jobs:
        validate_certs:
        vm_id:
        vm_name:
