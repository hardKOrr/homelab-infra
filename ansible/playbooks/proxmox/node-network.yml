---
# https://docs.ansible.com/projects/ansible/latest/collections/community/proxmox/proxmox_node_network_module.html

- name: "node-network.yml"
  gather_facts: true
  hosts: proxmox
  vars:

  tasks:
    - name: "Manage Proxmox Node Network"
      community.general.proxmox_node_network:
        api_host:
        api_password:
        api_port: 8006
        api_token_id:
        api_token_secret:
        api_user:
        autostart:
        bond_mode:
        bond_primary:
        bond_xmit_hash_policy:
        bridge_ports:
        bridge_vids:
        bridge_vlan_aware:
        cidr:
        cidr6:
        comments:
        gateway:
        gateway6:
        iface:
        iface_type:
        mtu:
        node:
        ovs_bonds:
        ovs_bridge:
        ovs_options:
        ovs_ports:
        ovs_tag:
        slaves:
        state:
        validate_certs:
        vlan_raw_device:
