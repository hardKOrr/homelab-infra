---
# https://docs.ansible.com/projects/ansible/latest/collections/community/proxmox/proxmox_node_info_module.html

- name: "node-info.yml"
  gather_facts: true
  hosts: proxmox
  vars:

  tasks:
    - name: "Get Proxmox Node Info"
      community.general.proxmox_node_info:
        api_host:
        api_password:
        api_port: 8006
        api_token_id:
        api_token_secret:
        api_user:
        validate_certs:
