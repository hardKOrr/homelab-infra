---
# https://docs.ansible.com/projects/ansible/latest/collections/community/docker/docker_volume_info_module.html

- name: "volume-info.yml"
  gather_facts: true
  hosts: docker
  vars:

  tasks:
    - name: "Get Docker Volume Info"
      community.docker.docker_volume_info:
        debug:
        timeout:
        tls:
        tls_hostname:
        use_ssh_client:
