---
# TODO: Register an app route with Nginx Proxy Manager via its REST API.
#
# Expected inputs (vars passed by calling playbook):
#   wiring_app_name:       the app instance name
#   wiring_upstream_host:  IP or hostname of the app container
#   wiring_upstream_port:  port the app listens on
#   wiring_domain:         full domain (e.g. radarr-4k.homelab.example.com)
#
# Reads from homelabinfra_infra:
#   homelabinfra_infra.nginx.api_url    (e.g. http://192.168.1.11:81/api)
#   homelabinfra_infra.nginx.api_token
#
# Steps:
#   1. Authenticate to NPM API (token stored in Vaultwarden, loaded from facts.yml)
#   2. Create proxy host entry via POST /api/nginx/proxy-hosts
#   3. Request SSL certificate if not already present
#
# Conditions: only runs when homelabinfra_infra.reverse_proxy.provider == 'nginx'
