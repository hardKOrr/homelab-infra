---
# Deploy Kometa as a scheduled Kubernetes workload. Its complete config.yml is authored
# by the operator because the collection and metadata policy is theirs, and because it
# contains credentials to the media services it drives.

- name: Kometa | Resolve the cluster and its API host
  ansible.builtin.include_tasks: ../../../tasks/kubernetes/resolve-cluster.yml

- name: Kometa | Ensure the instance namespace exists
  ansible.builtin.include_tasks: ../../../tasks/kubernetes/ensure-namespace.yml

- name: Kometa | Read the operator-owned configuration
  ansible.builtin.set_fact:
    _kometa_config_yml: "{{ app_config.app.config_yml | default('', true) }}"
  no_log: true

- name: Kometa | Require an operator-owned configuration
  ansible.builtin.assert:
    that: _kometa_config_yml | length > 0
    fail_msg: >-
      No Kometa config.yml is stored for {{ instance }}. Use Store Secret to write the
      complete YAML to homelab-infra/apps/{{ instance }}, field config_yml, then rerun
      this deployment.
  no_log: true

- name: Kometa | Validate the configuration is YAML
  ansible.builtin.set_fact:
    _kometa_config_document: "{{ _kometa_config_yml | from_yaml }}"
  no_log: true

- name: Kometa | Require a mapping configuration
  ansible.builtin.assert:
    that: _kometa_config_document is mapping
    fail_msg: >-
      homelab-infra/apps/{{ instance }} field config_yml must contain a YAML mapping.
  no_log: true

- name: Kometa | Sync the configuration into the namespace
  ansible.builtin.include_tasks: ../../../tasks/kubernetes/sync-secret.yml
  vars:
    k8s_secret_name: "{{ instance }}-config"
    k8s_secret_data:
      config.yml: "{{ _kometa_config_yml }}"
  no_log: true

- name: Kometa | Render the scheduled workload manifest
  ansible.builtin.set_fact:
    _kometa_manifest: "{{ lookup('ansible.builtin.template', 'manifest.yaml.j2') }}"

# Do not start a metadata-changing job outside the schedule the operator selected.
- name: Kometa | Apply the scheduled workload manifest
  ansible.builtin.include_tasks: ../../../tasks/kubernetes/apply-manifest.yml
  vars:
    k8s_manifest: "{{ _kometa_manifest }}"
