---
galaxy_info:
  role_name: APP_NAME
  description: "Deploys APP_NAME as a Docker container on a homelab-infra stack host"
  min_ansible_version: "2.15"

dependencies:
  - role: docker   # ensures Docker Engine is installed on the target host
