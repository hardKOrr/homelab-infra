{{ ansible_managed | comment }}
# Compose environment for the observability stack ({{ instance }}) — managed by
# homelab-infra. Mode 0600, root-owned. The password is generated once and reused
# on re-run; see the role header for how continuity works.
GF_ADMIN_USER={{ app_config.app.admin_user | default('admin') }}
GF_ADMIN_PASSWORD={{ _observability_admin_password }}
