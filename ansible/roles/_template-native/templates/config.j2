# APP_NAME configuration
# Managed by homelab-infra — changes made here will be overwritten on next deploy.
# Instance: {{ instance }}

# Add your app's config keys here using Jinja2 variables.
# Available vars: app_config.*, homelabinfra_infra.*, homelabinfra_config.*

# Example:
# listen_addr: "0.0.0.0:{{ app_config.app.port }}"
# data_dir: "{{ app_config.app.data_path }}"
