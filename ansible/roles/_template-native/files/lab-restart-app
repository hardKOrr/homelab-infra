#!/usr/bin/env bash
# lab-restart-app — restart this app's service.
#
# Exit 0: restarted successfully
# Exit non-zero: restart failed (systemctl exit code propagated)

set -euo pipefail

# TODO: replace APP_NAME with the systemd service name for this app.
# Example:
#   systemctl restart APP_NAME

echo "lab-restart-app not implemented for this app" >&2
exit 1
