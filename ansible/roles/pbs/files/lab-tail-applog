#!/usr/bin/env bash
# lab-tail-applog — stream recent log output for Proxmox Backup Server.
#
# Usage: lab-tail-applog [lines]
#   lines: number of recent lines to show (default: 50)

set -euo pipefail

LINES="${1:-50}"

journalctl -u proxmox-backup -u proxmox-backup-proxy -n "$LINES" --no-pager
