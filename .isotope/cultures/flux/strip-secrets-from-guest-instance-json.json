{"acceptances":[],"assays":[],"changes":[],"decisions":[],"depends_on":[],"events":[{"after_hash":"sha256:c7c5632d49f78842201afef91075d9d01789d8ceaa285fac744aae7d60f88051","before_hash":null,"entity_id":"analysis","entity_kind":"analysis","id":"E1","operation":"add","order":1,"owner":"operate","prior_revision":"sha256:8404e3cf463807343a076928fc49fbb9fea8b1db5483825be8605e72b0b7dcff","reaction":"analyze","reason":"Recorded the Analyze structure and flux promotion.","schema_version":"2"}],"goal":"The instance JSON persisted into each provisioned LXC and VM excludes all auth material (Proxmox `api_token_secret`/`api_token_id`, LXC root `password`, and `pubkey`/`sshkeys`) and is written to a sane in-guest path with 0600 permissions, leaving the module calls and status/exec waits unchanged.","matter":{"content":"### Goal\nStop writing the Proxmox API token and LXC root password into every provisioned guest: strip auth\nkeys from the instance JSON that `lxc-create.yml` and `vm-create.yml` persist inside the guest,\nand fix the odd `/root/home` destination path.\n\n### Context\n`ansible/tasks/proxmox/lxc-create.yml:96-103` pipes `homelabinfra_instance.lxc | to_nice_json`\ninto the new container at `/root/home/homelabinfra_instance.lxc.json`;\n`ansible/tasks/proxmox/vm-create.yml:100-107` does the same via `qm guest exec` for VMs. That\ndict is the full module-args dict built at lxc-create.yml:18-29 / vm-create.yml:16-31 and\ncontains `api_token_secret` (full Proxmox API control) and, for LXC, `password` (root password,\ndefault `changeme` from `vars/homelabinfra-defaults.yml:12`). The file is written with default\numask (world-readable). Per specs/secrets-handling.md, no secret may be persisted into a managed\nguest.\n\nFix shape: filter the dict before dumping — drop at least `api_token_secret`, `api_token_id`,\n`password`, `pubkey`/`sshkeys` (e.g. `dict2items | rejectattr('key', 'in', <denylist>) |\nitems2dict`), write with mode 0600, and settle the destination path (`/root/home/` is almost\ncertainly a typo for `/root/`; nothing else in the repo reads this file yet — grep to confirm\nbefore choosing). Related: meta slice 003 will replace the dict-splat module call with an\nallowlist; this item only sanitizes the persisted copy and should not restructure the module\ncall.\n\n### Acceptance criteria\n- The JSON written into a guest by `lxc-create.yml` and `vm-create.yml` contains no\n  `api_token_secret`, `api_token_id`, `password`, or SSH key material (checkable from the diff:\n  the denylist/filter is applied in both files).\n- The in-guest file lands at a sane path with 0600 permissions.\n- No other behavior of the create tasks changes (module call, waits untouched).\n- The `lint` gate from `.claude/build.yml` passes on the touched files.","source":".claude/plans/.intake-backlog-dump.md"},"prerequisites":["ansible/tasks/proxmox/lxc-create.yml (task 'Write LXC instance data to /root/home', currently lines 145-153) pipes `homelabinfra_instance.lxc | to_nice_json` into the guest via `pct exec`, and that dict carries `api_token_secret`, `api_token_id`, `password`, and `pubkey`.","ansible/tasks/proxmox/vm-create.yml (task 'Write VM instance data to /root/home', currently lines 165-173) pipes `homelabinfra_instance.vm | to_nice_json` into the guest via `qm guest exec`, and that dict carries `api_token_secret`, `api_token_id`, and `sshkeys`.","Both writers land the file under `/root/home/` and use a plain `cat` redirect, so the file inherits default umask (world-readable) rather than an explicit 0600 mode.","No file in the repository reads `homelabinfra_instance.lxc.json` or `homelabinfra_instance.vm.json`; the only references are the two writer tasks, so the in-guest destination path can change without breaking any consumer.","The `lint` and `test` gates referenced by the acceptance criteria are registered in `.isotope/isotope.json` and runnable via `.claude/gate/lint.sh` and `.claude/gate/test.sh`."],"rounds":[],"schema_version":"2","slug":"strip-secrets-from-guest-instance-json","spec_provenance":[".claude/specs/secrets-handling.md"],"type":"fix"}
