{"acceptances":[],"assays":[],"changes":[],"decisions":[],"depends_on":[],"events":[{"after_hash":"sha256:838207bf70c01882568c085d035c015e6815d7d29253f621fdd7472e5eb1c775","before_hash":null,"entity_id":"analysis","entity_kind":"analysis","id":"E1","operation":"add","order":1,"owner":"operate","prior_revision":"sha256:d10cf450c24634f5c494d6755473b334a7d79b48fa9be37ba606a8339a577850","reaction":"analyze","reason":"Recorded the Analyze structure and flux promotion.","schema_version":"2"}],"goal":"Make ansible/tasks/stack/find-or-create-host.yml produce a host that can actually run Docker apps: an underscore-free hostname with the tag kept verbatim, the keyctl (plus existing nesting) LXC feature, and Docker Engine installed on first creation.","matter":{"content":"### Goal\nMake `ansible/tasks/stack/find-or-create-host.yml` produce a host that can actually run Docker\napps: valid hostname, keyctl/nesting features, and Docker Engine installed on first creation.\n\n### Context\nThe first Docker app deploy targeting a new stack creates the stack host via\n`find-or-create-host.yml`, then Play 2 of `playbooks/apps/_template.yml` goes straight from\n`guest-bootstrap` to `community.docker.docker_compose_v2`. Three gaps in the create path\n(lines 49-99):\n1. **Invalid hostname** — line 58 sets `hostname: stack_name`, but stack names use underscores\n   (`media_stack`, matching Proxmox tag/group conventions like `tag_media_stack`), and\n   underscores are invalid in hostnames; `pct create` rejects them. Hostname needs\n   `stack_name | replace('_', '-')` while the tag (line 59) keeps the underscore form —\n   the inventory group `tag_<stack_name>` is how the host is found on subsequent deploys\n   (line 23), so the tag must stay verbatim.\n2. **Missing container features** — Docker-on-LXC needs keyctl (and nesting, which\n   `vars/homelabinfra-defaults.yml:15-16` provides). The keyctl logic already exists in\n   `playbooks/docker/create-docker-host.yml:31-44` (handles both mapping and list shapes of\n   `proxmox.lxc.features`); it must also run on the find-or-create create path.\n3. **Docker never installed** — nothing applies the `docker` role to a freshly created stack\n   host. Either find-or-create marks the new host so Play 2 can apply the role conditionally, or\n   the app template's Play 2 applies `roles/docker` idempotently before the app role for Docker\n   apps. Note find-or-create runs on the proxmox node (Play 1) and cannot itself run the role on\n   the guest — the handoff to Play 2 is via `add_host` hostvars (lines 92-99), which is the\n   sanctioned cross-play channel (architecture \"Cross-play handoff\" seam).\n\nThe existing-host path (lines 26-46) must stay untouched except as needed for the group/tag\nlookup consistency.\n\n### Acceptance criteria\n- A created stack host's hostname contains no underscores; its Proxmox tags still include the\n  verbatim `stack_name` so `groups['tag_' + stack_name]` finds it on the next deploy.\n- The keyctl feature merge (same semantics as `create-docker-host.yml:31-44`) is applied on the\n  find-or-create create path.\n- After a first-ever deploy of a Docker app to a new stack, Docker Engine is present before the\n  app's compose tasks run, without a separate manual job (checkable from the diff: the role\n  application path exists and is conditional/idempotent).\n- The `lint` gate from `.claude/build.yml` passes on the touched files.","source":".claude/plans/.intake-backlog-dump.md"},"prerequisites":["ansible/tasks/stack/find-or-create-host.yml exists and its create block sets hostname: stack_name verbatim (line 58), applies no keyctl feature, and never applies the docker role.","ansible/playbooks/docker/create-docker-host.yml contains the reusable keyctl feature-merge that handles both the mapping and list shapes of proxmox.lxc.features (lines 28-40).","ansible/vars/homelabinfra-defaults.yml sets the LXC nesting=1 feature, so keyctl is the only container feature still missing for Docker-on-LXC.","ansible/playbooks/apps/_template.yml Play 2 goes straight from guest-bootstrap to the app role and applies no docker role, so a freshly created stack host has no Docker Engine.","ansible/roles/docker exists and installs Docker Engine idempotently, so the create path can reuse it rather than defining a new role."],"rounds":[],"schema_version":"2","slug":"make-stack-host-docker-ready","spec_provenance":[".claude/specs/one-click-idempotent.md — one click must yield a running, wired app with no manual step; governs Docker Engine being present before the first app's compose tasks run",".claude/architecture.md 'Cross-play handoff' seam — a newly created stack host reaches Play 2 only through add_host hostvars, so the Docker-role handoff must ride that sanctioned channel",".claude/architecture.md 'Proxmox boundary' seam — find-or-create runs on the proxmox node in Play 1 and cannot itself run the docker role on the guest, which is what forces the Play 2 handoff",".claude/specs/namespace-merge-discipline.md — the hostname/tag rewrite and the keyctl-feature merge on the create path must use combine(recursive=True), never a bare namespace assignment"],"type":"fix"}
