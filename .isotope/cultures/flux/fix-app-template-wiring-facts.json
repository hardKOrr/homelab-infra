{"acceptances":[],"assays":[],"changes":[],"decisions":[],"depends_on":[],"events":[{"after_hash":"sha256:c7307f65a49acafebc27ba708f4e109598c2bd12744dcaf0cf52113e5e74c60d","before_hash":null,"entity_id":"analysis","entity_kind":"analysis","id":"E1","operation":"add","order":1,"owner":"operate","prior_revision":"sha256:835b6320a4355af0bb86714646dda14cae4eb98176e5051c00775d15d0e6210b","reaction":"analyze","reason":"Recorded the Analyze structure and flux promotion.","schema_version":"2"}],"goal":"Make every wiring_* contract variable in Play 3 (\"Wire\") of ansible/playbooks/apps/_template.yml resolve from values reachable in the wiring play's own localhost scope -- the Docker stack-host IP for stack apps and the app LXC IP for native apps, with homelabinfra_infra loaded from config/.generated/facts.yml tolerating absence -- so both PATH A and PATH B yield correct reverse-proxy, SSO, uptime, and DNS registration.","matter":{"content":"### Goal\nFix Play 3 (\"Wire\") of `ansible/playbooks/apps/_template.yml` so the `wiring_*` contract\nvariables resolve: they currently read facts that were set on the Proxmox node's host scope and\nare undefined on `localhost`.\n\n### Context\nFacts are host-scoped. Play 1 runs on `proxmox_nodes` and sets `homelabinfra_instance` and\n`app_config` there, passing them to the deploy target via `add_host` hostvars\n(`tasks/stack/find-or-create-host.yml:38-46,92-99`). Play 3 (`_template.yml:117-163`) runs on\n`hosts: localhost` and declares:\n- `wiring_upstream_host: \"{{ homelabinfra_instance.network.ip_address }}\"` (line 126) — undefined\n  on localhost; and for Docker apps on an *existing* stack host, `network.ip_address` was never\n  set anyway (the existing-host path only sets `homelabinfra_instance.stack.ip_address`,\n  find-or-create-host.yml:26-35). The correct upstream for Docker apps is the stack host IP.\n- `wiring_upstream_port: \"{{ app_config.app.port }}\"` (line 127) — `app_config` is also a Play 1\n  fact, undefined on localhost.\n- `wiring_domain`/`wiring_monitor_url` use `homelabinfra_infra.domain` (lines 128, 130) — Play 3\n  never loads `config/.generated/facts.yml`, and the facts.yml shape is owned by meta slice 000's\n  contract (which does plan a top-level `domain` key). Load it like Play 2 does\n  (`_template.yml:96-100`).\n\nFix shape (pick one, apply consistently): reference Play 1's facts explicitly via\n`hostvars[groups['proxmox_nodes'][0]].homelabinfra_instance...`, or extend the `add_host` calls\nto also stash what Play 3 needs (e.g. a `wiring` dict on the deploy host, read back via\n`hostvars[groups['app_deploy'][0]]`). The template is the contract every future app playbook is\ncopied from (per `playbooks/apps/README.md`), so whichever shape lands must work for both the\nDocker path (PATH A, stack host) and the commented native-LXC path (PATH B, lines 66-81), and the\nPATH B comment block must be updated to match.\n\n### Acceptance criteria\n- Every `wiring_*` variable in Play 3 resolves from values reachable in Play 3's scope\n  (hostvars reads or loaded files — no bare Play 1 fact names), for both PATH A and PATH B.\n- Docker apps wire the stack host's IP as upstream; native apps wire the app LXC's IP.\n- Play 3 loads `homelabinfra_infra` from `config/.generated/facts.yml` (tolerating absence, per\n  specs/provider-noop-wiring.md) before referencing it.\n- The `lint` gate from `.claude/build.yml` passes on the touched file.","source":".claude/plans/.intake-backlog-dump.md"},"prerequisites":["ansible/playbooks/apps/_template.yml Play 3 (\"Wire\") runs on hosts: localhost and is the contract template every app playbook is copied from (playbooks/apps/README.md).","ansible/tasks/stack/find-or-create-host.yml publishes the Docker stack host IP only as homelabinfra_instance.stack.ip_address (existing-host path, lines 26-36), so wiring_upstream_host reading homelabinfra_instance.network.ip_address is undefined for Docker apps on an existing stack host.","config/.generated/facts.yml is the sole source of homelabinfra_infra.domain and may be absent on a fresh clone, so Play 3 must load it tolerating absence before referencing it.","The verifying gate is the Isotope lint gate (.isotope/isotope.json -> .claude/gate/lint.sh); no .claude/build.yml exists in the tree."],"rounds":[],"schema_version":"2","slug":"fix-app-template-wiring-facts","spec_provenance":[".claude/specs/provider-noop-wiring.md (wiring tasks consume only the documented wiring_* contract and homelabinfra_infra; the calling playbook must supply those from values visible in its own play scope, and an absent provider/facts.yml is a no-op)",".claude/architecture.md (Seams -> Cross-play handoff: facts are host-scoped; the only sanctioned way to move state between plays is add_host hostvars or hostvars[...] reads)",".claude/specs/config-layering.md (homelabinfra_infra shape loaded from config/.generated/facts.yml, the source of the domain key the wiring reads)"],"type":"fix"}
